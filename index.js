// Generated by CoffeeScript 2.0.0
(function() {
  var http, myIP;

  myIP = require('my-ip');

  http = require('http');

  exports.getPublicIp = function(o, cb) {
    var e, ip, req;
    ip = '';
    try {
      process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0";
      return req = http.get('http://myexternalip.com/raw', function(res) {
        res.setEncoding('utf8');
        res.on('data', function(chunk) {
          return ip += chunk;
        });
        res.on('end', function() {
          return cb(null, ip);
        });
        res.on('error', function(e) {
          return cb(null, 'unknown');
        });
        req.on('error', function(e) {
          return cb(null, 'unknown');
        });
        return req.setTimeout(3000, function() {
          return req.destroy();
        });
      });
    } catch (error) {
      e = error;
      return cb(e);
    }
  };

  exports.getPrivateIp = function(o, cb) {
    var e, ip;
    ip = null;
    try {
      ip = myIP(null, true);
      return cb(null, ip);
    } catch (error) {
      e = error;
      return cb(e);
    }
  };

}).call(this);

//# sourceMappingURL=index.js.map
